name: Build → Test Installers (DEV)

on:
  push:
    branches:
      - dev
  workflow_dispatch:  # Manual trigger for dev testing
    inputs:
      refresh_lock:
        description: 'Refresh requirements-lock.txt'
        required: false
        default: false
        type: boolean
      test_installers:
        description: 'Run installer tests'
        required: false
        default: true
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SHORT_SHA: ${{ github.sha }}

jobs:
  # =============================================================================
  # Prepare version info
  # =============================================================================
  prepare:
    name: Prepare Build Info
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      short_sha: ${{ steps.version.outputs.short_sha }}
      build_date: ${{ steps.version.outputs.build_date }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Extract version info
        id: version
        run: |
          VERSION=$(grep -oP 'version\s*=\s*"\K[^"]+' pyproject.toml | head -1)
          SHORT_SHA=${GITHUB_SHA::7}
          BUILD_DATE=$(date +'%Y-%m-%d')
          echo "version=$VERSION-dev" >> $GITHUB_OUTPUT
          echo "short_sha=$SHORT_SHA" >> $GITHUB_OUTPUT
          echo "build_date=$BUILD_DATE" >> $GITHUB_OUTPUT
          echo "🔧 DEV BUILD | Version: $VERSION-dev | SHA: $SHORT_SHA | Date: $BUILD_DATE"

  # =============================================================================
  # Ubuntu/Debian Package Build & Test
  # =============================================================================
  build-deb:
    name: Build & Test Debian Package
    runs-on: ubuntu-22.04
    needs: prepare
    
    steps:
      - name: Free up disk space
        run: |
          echo "🧹 Freeing up disk space..."
          df -h
          echo ""
          echo "Removing unnecessary packages..."
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo apt-get autoremove -y
          sudo apt-get clean
          echo ""
          echo "✅ Disk space after cleanup:"
          df -h
      
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev
      
      - name: Build .deb package
        run: |
          chmod +x ./installers/_builds/ubuntu/build_deb.sh
          # Always refresh lock file to ensure Python 3.10 compatibility
          ./installers/_builds/ubuntu/build_deb.sh --refresh-lock
      
      - name: Test .deb package structure
        run: |
          echo "🔍 Testing .deb package structure..."
          DEB_FILE=$(ls installers/releases/*.deb | head -1)
          
          if [ ! -f "$DEB_FILE" ]; then
            echo "❌ ERROR: .deb file not found"
            exit 1
          fi
          
          echo "📦 Package: $(basename $DEB_FILE)"
          
          # Check package info
          dpkg-deb --info "$DEB_FILE"
          
          # Check package contents
          echo ""
          echo "📂 Package contents:"
          dpkg-deb --contents "$DEB_FILE"
          
          echo "✅ .deb package validation passed"
      
      - name: Test installation (silent mode)
        if: github.event.inputs.test_installers != 'false'
        run: |
          echo "🧪 Testing .deb installation (silent mode)..."
          DEB_FILE=$(ls installers/releases/*.deb | head -1)
          
          echo "📦 Installing package: $(basename $DEB_FILE)"
          
          # Python 3.10+ is already available on Ubuntu 22.04+
          
          # Install the package
          sudo dpkg -i "$DEB_FILE" || true
          
          # Fix any dependency issues
          sudo apt-get install -f -y
          
          # Verify installation
          if dpkg -l | grep -q ai-os; then
            echo "✅ Package installed successfully"
            
            # Check installed files
            echo ""
            echo "📂 Installed files:"
            dpkg -L ai-os | head -20
            
            # Check for essential directories and files
            if [ -d "/opt/ai-os" ]; then
              echo "✅ Found: /opt/ai-os directory"
              ls -la /opt/ai-os | head -10
            else
              echo "❌ ERROR: Missing /opt/ai-os directory"
              exit 1
            fi
            
            if [ -f "/usr/local/bin/aios" ] || [ -L "/usr/local/bin/aios" ]; then
              echo "✅ Found: /usr/local/bin/aios"
            else
              echo "⚠️  WARNING: Missing /usr/local/bin/aios"
            fi
            
            # Try to run aios --version if available
            if command -v aios &> /dev/null; then
              echo ""
              echo "🔍 Testing aios command:"
              aios --version || echo "⚠️  aios command exists but version check failed"
            fi
            
            # Clean up - remove the package
            echo ""
            echo "🧹 Cleaning up test installation..."
            sudo dpkg -r ai-os
            
            echo "✅ Installation test completed"
          else
            echo "❌ ERROR: Package installation failed"
            dpkg -l | grep ai-os || echo "Package not found in dpkg list"
            exit 1
          fi
      
      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: AI-OS-${{ needs.prepare.outputs.version }}-ubuntu-${{ needs.prepare.outputs.short_sha }}-DEV
          path: installers/releases/*.deb
          retention-days: 7
          if-no-files-found: error
  
  # =============================================================================
  # Prepare PPA Source Package (DEV)
  # =============================================================================
  prepare-ppa-dev:
    name: Prepare PPA Source Package (aios-dev)
    runs-on: ubuntu-22.04
    needs: [prepare, build-deb]
    if: success()
    outputs:
      dsc_file: ${{ steps.build_source.outputs.dsc_file }}
      changes_file: ${{ steps.build_source.outputs.changes_file }}
    
    steps:
      - name: Free up disk space
        run: |
          echo "🧹 Freeing up disk space..."
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo apt-get autoremove -y
          sudo apt-get clean
          df -h
      
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install Launchpad build tools
        run: |
          echo "🔧 Installing Launchpad build dependencies..."
          sudo apt-get update
          sudo apt-get install -y \
            devscripts \
            debhelper \
            dh-python \
            python3-all \
            python3-setuptools \
            dpkg-dev \
            fakeroot \
            lintian \
            dput \
            gnupg2
      
      - name: Import GPG key
        env:
          GPG_PRIVATE_KEY: ${{ secrets.LAUNCHPAD_GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.LAUNCHPAD_GPG_PASSPHRASE }}
        run: |
          echo "🔐 Importing GPG key..."
          
          if [ -z "$GPG_PRIVATE_KEY" ]; then
            echo "❌ GPG_PRIVATE_KEY secret not set"
            echo "::error title=GPG Key Missing::LAUNCHPAD_GPG_PRIVATE_KEY secret must be configured"
            exit 1
          fi
          
          echo "::add-mask::$GPG_PASSPHRASE"
          echo "$GPG_PRIVATE_KEY" | gpg --batch --quiet --import 2>/dev/null
          
          KEY_ID=$(gpg --list-secret-keys --keyid-format LONG 2>/dev/null | grep sec | awk '{print $2}' | cut -d'/' -f2 | head -1)
          KEY_ID_SHORT=$(echo "$KEY_ID" | tail -c 9)
          echo "key_id=$KEY_ID" >> $GITHUB_OUTPUT
          echo "✅ GPG Key imported (ID: *****$KEY_ID_SHORT)"
          
          if [ -n "$GPG_PASSPHRASE" ]; then
            echo "allow-preset-passphrase" >> ~/.gnupg/gpg-agent.conf
            gpg-connect-agent reloadagent /bye 2>/dev/null || true
          fi
      
      - name: Build source package for PPA
        id: build_source
        env:
          DEBEMAIL: "support@ai-os.invalid"
          DEBFULLNAME: "AI-OS Dev Team"
        run: |
          echo "📦 Building source package for PPA..."
          
          VERSION="${{ needs.prepare.outputs.version }}"
          PACKAGE_NAME="ai-os"
          BUILD_DIR="${PACKAGE_NAME}-${VERSION}"
          
          echo "Version: $VERSION"
          echo "Build directory: $BUILD_DIR"
          
          # Check if custom source package script exists
          if [ -f "installers/_builds/ubuntu/create_source_package.sh" ]; then
            chmod +x "installers/_builds/ubuntu/create_source_package.sh"
            "installers/_builds/ubuntu/create_source_package.sh"
          else
            echo "⚠️  Source package script not found, creating basic structure..."
            
            # Create properly named build directory
            mkdir -p "$BUILD_DIR"
            
            # Copy source files excluding build artifacts
            echo "📁 Copying source files..."
            rsync -av ./ "$BUILD_DIR/" \
              --exclude=".git" \
              --exclude="build" \
              --exclude="dist" \
              --exclude="*.egg-info" \
              --exclude="__pycache__" \
              --exclude=".venv" \
              --exclude="artifacts" \
              --exclude="${PACKAGE_NAME}-*" \
              --exclude="installers/_builds" \
              --exclude="installers/releases"
            
            # Create debian directory
            mkdir -p "$BUILD_DIR/debian"
            
            # Create debian/control
            printf '%s\n' \
              'Source: ai-os' \
              'Section: devel' \
              'Priority: optional' \
              'Maintainer: AI-OS Dev Team <support@ai-os.invalid>' \
              'Build-Depends: debhelper-compat (= 12), dh-python, python3-all (>= 3.10), python3-setuptools' \
              'Standards-Version: 4.5.1' \
              'Homepage: https://github.com/Wulfic/AI-OS' \
              '' \
              'Package: ai-os' \
              'Architecture: all' \
              'Depends: ${python3:Depends}, ${misc:Depends}, python3 (>= 3.10), python3-pip, python3-venv, ffmpeg' \
              'Description: AI Operating System - Recursive Hierarchical Reasoning Models (DEV)' \
              ' AI-OS provides advanced AI models with recursive hierarchical reasoning' \
              ' capabilities, supporting multiple modalities and adaptive learning.' \
              ' .' \
              ' This is a development build from the dev branch.' \
              > "$BUILD_DIR/debian/control"
            
            # Create debian/changelog
            printf '%s\n' \
              "ai-os ($VERSION-1) unstable; urgency=medium" \
              '' \
              "  * Development build $VERSION" \
              '  * See GitHub commits for changes' \
              '' \
              " -- AI-OS Dev Team <support@ai-os.invalid>  $(date -R)" \
              > "$BUILD_DIR/debian/changelog"
            
            # Create debian/rules
            printf '%s\n' \
              '#!/usr/bin/make -f' \
              '' \
              '%:' \
              '	dh $@ --with python3 --buildsystem=pybuild' \
              > "$BUILD_DIR/debian/rules"
            chmod +x "$BUILD_DIR/debian/rules"
            
            # Create debian/compat
            echo "12" > "$BUILD_DIR/debian/compat"
            
            # Create source tarball (.orig.tar.gz)
            # Use full version including -dev suffix
            echo "📦 Creating original source tarball: ${PACKAGE_NAME}_${VERSION}.orig.tar.gz"
            tar --exclude-vcs --exclude="$BUILD_DIR/debian" \
                -czf "${PACKAGE_NAME}_${VERSION}.orig.tar.gz" "$BUILD_DIR"
            
            # Verify tarball was created
            if [ ! -f "${PACKAGE_NAME}_${VERSION}.orig.tar.gz" ]; then
              echo "❌ Failed to create tarball"
              ls -la
              exit 1
            fi
            
            echo "✅ Tarball created: $(ls -lh ${PACKAGE_NAME}_${VERSION}.orig.tar.gz)"
            
            # Build source package
            echo "🔨 Building source package..."
            cd "$BUILD_DIR"
            debuild -S -sa -us -uc --no-check-builddeps 2>&1 || {
              echo "❌ debuild failed"
              exit 1
            }
            cd ..
          fi
          
          # Find generated files
          DSC_FILE=$(find . -name "*.dsc" | head -1)
          CHANGES_FILE=$(find . -name "*_source.changes" | head -1)
          
          if [ -z "$DSC_FILE" ] || [ -z "$CHANGES_FILE" ]; then
            echo "❌ Source package files not found"
            exit 1
          fi
          
          echo "dsc_file=$DSC_FILE" >> $GITHUB_OUTPUT
          echo "changes_file=$CHANGES_FILE" >> $GITHUB_OUTPUT
          echo "✅ Source package built successfully"
      
      - name: Sign source package
        env:
          GPG_PASSPHRASE: ${{ secrets.LAUNCHPAD_GPG_PASSPHRASE }}
        run: |
          echo "🔏 Signing source package..."
          echo "::add-mask::$GPG_PASSPHRASE"
          
          CHANGES_FILE="${{ steps.build_source.outputs.changes_file }}"
          
          if [ -n "$GPG_PASSPHRASE" ]; then
            echo "$GPG_PASSPHRASE" | debsign -p"gpg --batch --yes --quiet --passphrase-fd 0" "$CHANGES_FILE" 2>&1 | grep -v "gpg:\|Good signature" || true
          else
            debsign "$CHANGES_FILE" 2>&1 | grep -v "gpg:\|Good signature" || true
          fi
          
          echo "✅ Package signed successfully"
      
      - name: Validate package with lintian
        run: |
          echo "🔍 Validating package..."
          CHANGES_FILE="${{ steps.build_source.outputs.changes_file }}"
          lintian -I -E --pedantic "$CHANGES_FILE" || true
          echo "✅ Validation complete"
      
      - name: Upload PPA source artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ppa-source-package-dev
          path: |
            *.dsc
            *.tar.*
            *.changes
            *.buildinfo
          retention-days: 7

  # =============================================================================
  # Upload to PPA (DEV)
  # =============================================================================
  publish-ppa-dev:
    name: Upload to PPA (aios-dev)
    needs: [prepare, prepare-ppa-dev]
    runs-on: ubuntu-22.04
    
    steps:
      - name: Download source package
        uses: actions/download-artifact@v4
        with:
          name: ppa-source-package-dev
          path: package
      
      - name: Install dput
        run: |
          sudo apt-get update
          sudo apt-get install -y dput gnupg2
      
      - name: Configure dput for Launchpad
        run: |
          mkdir -p ~/.dput.d
          
          printf '%s\n' \
            '[DEFAULT]' \
            'default_host_main = ppa' \
            '' \
            '[ppa]' \
            'fqdn = ppa.launchpad.net' \
            'method = ftp' \
            'incoming = ~%(ppa)s/ubuntu' \
            'login = anonymous' \
            'allow_unsigned_uploads = 0' \
            > ~/.dput.cf
          
          echo "✅ dput configured"
      
      - name: Import GPG key for upload
        env:
          GPG_PRIVATE_KEY: ${{ secrets.LAUNCHPAD_GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.LAUNCHPAD_GPG_PASSPHRASE }}
        run: |
          echo "🔐 Importing GPG key for upload..."
          echo "::add-mask::$GPG_PASSPHRASE"
          echo "$GPG_PRIVATE_KEY" | gpg --batch --quiet --import 2>/dev/null
          echo "✅ GPG key ready for signing"
      
      - name: Upload to PPA
        run: |
          echo "📤 Uploading to ppa:wulfic/aios-dev..."
          
          cd package
          CHANGES_FILE=$(find . -name "*_source.changes" | head -1)
          
          if [ -z "$CHANGES_FILE" ]; then
            echo "❌ Source changes file not found"
            exit 1
          fi
          
          dput ppa:wulfic/aios-dev "$CHANGES_FILE"
          
          echo "✅ Package uploaded successfully to ppa:wulfic/aios-dev"
  
  # =============================================================================
  # Test PPA Installation (DEV)
  # =============================================================================
  test-ppa-install-dev:
    name: Test PPA Installation (aios-dev)
    runs-on: ubuntu-22.04
    needs: [prepare, publish-ppa-dev]
    if: success()
    
    steps:
      - name: Wait for PPA processing
        run: |
          echo "⏳ Waiting 5 minutes for PPA to process package..."
          sleep 300
      
      - name: Add PPA and install
        run: |
          echo "📥 Adding ppa:wulfic/aios-dev"
          sudo add-apt-repository -y ppa:wulfic/aios-dev
          sudo apt-get update
          
          echo "📦 Installing ai-os from PPA"
          sudo apt-get install -y ai-os
      
      - name: Verify installation
        run: |
          echo "✅ Verifying installation..."
          
          # Check if package is installed
          if dpkg -l | grep -q ai-os; then
            echo "✅ Package installed successfully"
          else
            echo "❌ Package not found"
            exit 1
          fi
          
          # Check if files exist
          if [ -d "/opt/ai-os" ]; then
            echo "✅ Found /opt/ai-os"
          else
            echo "❌ Missing /opt/ai-os"
            exit 1
          fi
          
          # Try to run aios command
          if command -v aios &> /dev/null; then
            echo "✅ aios command available"
            aios --version || echo "⚠️  Version check failed but command exists"
          else
            echo "⚠️  aios command not found in PATH"
          fi

  # =============================================================================
  # Windows Installer Build & Test
  # =============================================================================
  build-windows:
    name: Build & Test Windows Installer
    runs-on: windows-latest
    needs: prepare
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install Inno Setup
        run: |
          # Download Inno Setup 6 with retry logic
          $innoUrls = @(
            "https://jrsoftware.org/download.php/is.exe",
            "https://files.jrsoftware.org/is/6/innosetup-6.3.3.exe"
          )
          $innoInstaller = "$env:TEMP\innosetup.exe"
          $maxRetries = 3
          $downloaded = $false
          
          foreach ($url in $innoUrls) {
            for ($i = 1; $i -le $maxRetries; $i++) {
              try {
                Write-Host "Downloading Inno Setup from $url (attempt $i/$maxRetries)..."
                Invoke-WebRequest -Uri $url -OutFile $innoInstaller -UseBasicParsing -TimeoutSec 30
                $downloaded = $true
                Write-Host "✅ Download successful"
                break
              } catch {
                Write-Host "⚠️  Download failed: $($_.Exception.Message)"
                if ($i -lt $maxRetries) {
                  Start-Sleep -Seconds 5
                }
              }
            }
            if ($downloaded) { break }
          }
          
          if (-not $downloaded) {
            Write-Error "Failed to download Inno Setup from all sources"
            exit 1
          }
          
          Write-Host "Installing Inno Setup silently..."
          Start-Process -FilePath $innoInstaller -ArgumentList "/VERYSILENT", "/SUPPRESSMSGBOXES", "/NORESTART", "/SP-" -Wait
          
          # Add to PATH for this session
          $innoPath = "C:\Program Files (x86)\Inno Setup 6"
          echo "$innoPath" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          
          Write-Host "✅ Inno Setup installed successfully"
        shell: pwsh
      
      - name: Build Windows installer
        run: |
          & .\installers\_builds\windows\build_windows_installer.ps1
        shell: pwsh
      
      - name: Test installer structure
        run: |
          Write-Host "🔍 Testing Windows installer structure..."
          
          $installerFile = Get-ChildItem -Path "installers\releases\*.exe" | Select-Object -First 1
          
          if (-not $installerFile) {
            Write-Host "❌ ERROR: Installer file not found"
            exit 1
          }
          
          Write-Host "📦 Installer: $($installerFile.Name)"
          Write-Host "📊 Size: $([math]::Round($installerFile.Length / 1MB, 2)) MB"
          
          # Check if file is a valid PE executable
          $fileHeader = [System.IO.File]::ReadAllBytes($installerFile.FullName)[0..1]
          if ($fileHeader[0] -eq 0x4D -and $fileHeader[1] -eq 0x5A) {
            Write-Host "✅ Valid Windows executable (PE format)"
          } else {
            Write-Host "❌ ERROR: Not a valid Windows executable"
            exit 1
          }
          
          Write-Host "✅ Installer validation passed"
        shell: pwsh
      
      - name: Test installation (silent mode)
        if: github.event.inputs.test_installers != 'false'
        run: |
          Write-Host "🧪 Testing Windows installation (silent mode)..."
          
          # Check if running as admin
          $isAdmin = ([Security.Principal.WindowsPrincipal] [Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
          Write-Host "🔐 Running as admin: $isAdmin"
          
          $installerFile = Get-ChildItem -Path "installers\releases\*.exe" | Select-Object -First 1
          $testInstallDir = "$env:TEMP\AI-OS-Test-$([guid]::NewGuid().ToString().Substring(0,8))"
          $logFile = "$env:TEMP\aios-install-test.log"
          
          Write-Host "📦 Installer: $($installerFile.Name)"
          Write-Host "📂 Test installation directory: $testInstallDir"
          
          # Run installer in silent mode with custom directory
          # GitHub Actions runners run with admin privileges by default
          $arguments = "/VERYSILENT", "/SUPPRESSMSGBOXES", "/NORESTART", "/DIR=$testInstallDir", "/LOG=$logFile"
          $process = Start-Process -FilePath $installerFile.FullName -ArgumentList $arguments -PassThru -Wait -NoNewWindow
          
          Write-Host "📊 Installer exit code: $($process.ExitCode)"
          
          if ($process.ExitCode -ne 0) {
            Write-Host "⚠️  WARNING: Installation may have failed"
            if (Test-Path $logFile) {
              Write-Host ""
              Write-Host "📋 Last 50 lines of installer log:"
              Get-Content $logFile -Tail 50 | Write-Host
            }
          }
          
          # Check if installation directory was created
          Start-Sleep -Seconds 2  # Give the installer time to complete
          
          if (Test-Path $testInstallDir) {
            Write-Host ""
            Write-Host "✅ Installation directory created"
            
            # List top-level files and directories
            Write-Host ""
            Write-Host "📂 Top-level files and directories:"
            Get-ChildItem -Path $testInstallDir | Format-Table Name, Length, Mode -AutoSize
            
            # Check for essential files
            Write-Host ""
            Write-Host "🔍 Checking for essential files..."
            $essentialFiles = @(
              "launcher.bat",
              "pyproject.toml",
              "LICENSE"
            )
            
            $missingCount = 0
            foreach ($file in $essentialFiles) {
              $filePath = Join-Path $testInstallDir $file
              if (Test-Path $filePath) {
                Write-Host "  ✅ Found: $file"
              } else {
                Write-Host "  ❌ Missing: $file"
                $missingCount++
              }
            }
            
            # Clean up test installation
            Write-Host ""
            Write-Host "🧹 Cleaning up test installation..."
            Remove-Item -Path $testInstallDir -Recurse -Force -ErrorAction SilentlyContinue
            
            if ($missingCount -eq 0) {
              Write-Host "✅ Installation test completed successfully"
            } else {
              Write-Host "❌ ERROR: Installation test failed - $missingCount essential files missing"
              exit 1
            }
          } else {
            Write-Host ""
            Write-Host "❌ ERROR: Installation directory not found"
            Write-Host "This typically means the installer failed to run or was unable to create the directory."
            
            if ($process.ExitCode -ne 0) {
              Write-Host "Installer exit code: $($process.ExitCode)"
            }
            
            Write-Host "❌ Installation test failed"
            exit 1
          }
        shell: pwsh
      
      - name: Upload Windows installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: AI-OS-${{ needs.prepare.outputs.version }}-windows-${{ needs.prepare.outputs.short_sha }}-DEV
          path: installers/releases/*.exe
          retention-days: 7
          if-no-files-found: error

  # =============================================================================
  # Summary Report
  # =============================================================================
  summary:
    name: Build Summary
    needs: [prepare, build-deb, build-windows, prepare-ppa-dev, publish-ppa-dev, test-ppa-install-dev]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Generate summary
        run: |
          echo "# 🔧 Dev Installer Build → Test → Publish Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ needs.prepare.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ needs.prepare.outputs.short_sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Build Date:** ${{ needs.prepare.outputs.build_date }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Pipeline Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔨 Build Debian | ${{ needs.build-deb.result == 'success' && '✅' || needs.build-deb.result == 'skipped' && '⏭️' || '❌' }} ${{ needs.build-deb.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔨 Build Windows | ${{ needs.build-windows.result == 'success' && '✅' || needs.build-windows.result == 'skipped' && '⏭️' || '❌' }} ${{ needs.build-windows.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 Prepare PPA Source | ${{ needs.prepare-ppa-dev.result == 'success' && '✅' || needs.prepare-ppa-dev.result == 'skipped' && '⏭️' || '❌' }} ${{ needs.prepare-ppa-dev.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📤 Upload to PPA (aios-dev) | ${{ needs.publish-ppa-dev.result == 'success' && '✅' || needs.publish-ppa-dev.result == 'skipped' && '⏭️' || '❌' }} ${{ needs.publish-ppa-dev.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🧪 Test PPA Install | ${{ needs.test-ppa-install-dev.result == 'success' && '✅' || needs.test-ppa-install-dev.result == 'skipped' && '⏭️' || '❌' }} ${{ needs.test-ppa-install-dev.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 PPA Repository (Development)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "After PPA build completes (5-15 minutes), install via:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "sudo add-apt-repository ppa:wulfic/aios-dev" >> $GITHUB_STEP_SUMMARY
          echo "sudo apt update" >> $GITHUB_STEP_SUMMARY
          echo "sudo apt install ai-os" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️  **Note:** This is a development build from the dev branch. Artifacts are retained for 7 days." >> $GITHUB_STEP_SUMMARY
