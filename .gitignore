# Local artifacts/logs (kept if explicitly added)
aios.log
artifacts/*.out.log
artifacts/*.err.log
debug_html_output.html

# Centralized logs directory
logs/
!logs/README.md

# Test outputs and temporary files
artifacts/test_*.jsonl
artifacts/test_*.log
artifacts/*.log
test_output.txt
test_*.txt

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
ENV/
/.venvs/
.venv-dml/
.dmlvenv/
*.env

# Python packaging
*.egg-info/
.build/

dist/
build/

# Test/cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
artifacts/test_results/
test_results/

# IDE/editor
.idea/

# Backup files
*.backup
*~
*.bak
*.swp
*.swo

# Project data/cache
.cache/
*.log
artifacts/brains/
artifacts/diagnostics/
artifacts/experts/

# Only ignore specific checkpoint patterns if they're temporary
artifacts/checkpoints/*-step*-*.npz

# Ignore all parallel checkpoint directories (training artifacts)
**/parallel_checkpoints/

# Generated halting-head weights
artifacts/**/q_head.pt

# Still track registry metadata if present
!artifacts/brains/masters.json
!artifacts/brains/pinned.json

# Ignore installer builds
installers/releases/*.deb
installers/releases/*.exe
installers/releases/*.AppImage
installers/releases/*.dmg
installers/_builds/ubuntu/*.snap
installers/_builds/ubuntu/parts/
installers/_builds/ubuntu/prime/
installers/_builds/ubuntu/stage/
installers/_builds/ubuntu/snap/

# Local training outputs (ignored)
training_data/
**/metrics.jsonl

# Diagnostics files (generated with user-specific paths)
artifacts/diagnostics/diagnostics_latest.json
artifacts/diagnostics/diagnostics_latest.txt
artifacts/diagnostics/analytics_events.jsonl

# Evaluation results (contain user-specific paths)
artifacts/evaluation/

# User-specific GUI state
src/gui_state.json

# GPU metrics and monitoring
**/gpu_metrics.jsonl
**/gpu_metrics.log
**/gpu_stats.jsonl
**/gpu_stats.log
artifacts/gpu_metrics/
gpu_metrics/

# Optimization testing results
artifacts/optimization/
**/progressive_results_*.json
**/train_*.jsonl
**/stop_*.flag

# OS
.DS_Store
Thumbs.db

# Track workspace settings
!.vscode/settings.json

# Note: .whl files can be large but not tracking in LFS for now
flash_attn-*.whl
*.whl
artifacts/diagnostics/device_info_cache.json
artifacts/diagnostics/device_info_cache.json
artifacts/diagnostics/device_info_cache.json
training_datasets/huggingface_documentation-images/dataset_info.json
training_datasets/huggingface_documentation-images/state.json
installers/releases/ai-os_1.0.0_amd64.snap
installers/releases/apt/pool/main/a/ai-os/ai-os_1.0.0_amd64.deb
