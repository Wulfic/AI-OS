# External Projects
EXTERNAL_PROJECTS/

# Local artifacts/logs (kept if explicitly added)
aios.log
artifacts/*.out.log
artifacts/*.err.log
debug_html_output.html

# MkDocs generated site
site/

# Centralized logs directory
logs/
!logs/README.md

# Test outputs and temporary files
artifacts/test_*.jsonl
artifacts/test_*.log
artifacts/*.log
test_output.txt
test_*.txt

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
ENV/
/.venvs/
.venv-dml/
.dmlvenv/
*.env

# Python packaging
*.egg-info/
.build/

dist/
build/

# Test/cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
artifacts/test_results/
test_results/

# IDE/editor
.idea/

# Backup files
*.backup
*~
*.bak
*.bak_*
*.swp
*.swo

# Project data/cache
.cache/
*.log
artifacts/brains/
artifacts/diagnostics/
# Keep diagnostic tooling/configs in git; ignore run outputs.
!artifacts/diagnostics/*.py
!artifacts/diagnostics/*.yaml
!artifacts/diagnostics/*.yml
!artifacts/diagnostics/*.txt
!artifacts/diagnostics/*.md
!artifacts/diagnostics/**/validate_*.py
!artifacts/diagnostics/**/adaptive_lr_*_config.yaml
!artifacts/diagnostics/**/adaptive_lr_*_dataset.txt
artifacts/experts/

# Only ignore specific checkpoint patterns if they're temporary
artifacts/checkpoints/*-step*-*.npz

# Ignore all parallel checkpoint directories (training artifacts)
**/parallel_checkpoints/

# Generated halting-head weights
artifacts/**/q_head.pt

# Ignore installer builds
installers/releases/
installers/_builds/ubuntu/.src-build/
installers/_builds/ubuntu/*.snap
installers/_builds/ubuntu/parts/
installers/_builds/ubuntu/prime/
installers/_builds/ubuntu/stage/
installers/_builds/ubuntu/snap/

# Local training outputs (ignored)
training_data/
**/metrics.jsonl

# Live training tensor artifacts (do not commit)
artifacts/training_datasets/**/*.safetensors
artifacts/training_datasets/**/*.safetensors.*

# Chunk tracker state backups (generated)
**/chunk_tracker_state.json.bak_*
**/chunk_tracker_state.json.bak_*.json

# Diagnostics files (generated with user-specific paths)
artifacts/diagnostics/diagnostics_latest.json
artifacts/diagnostics/diagnostics_latest.txt
artifacts/diagnostics/analytics_events.jsonl

# Evaluation results (contain user-specific paths)
artifacts/evaluation/

# User-specific GUI state
src/gui_state.json
gui_state.json

# Session/lock files (user-specific)
cache/logging/.session_state.json
cache/logging/locks/

# User-specific config changes
config/default.yaml

# GPU metrics and monitoring
**/gpu_metrics.jsonl
**/gpu_metrics.log
**/gpu_stats.jsonl
**/gpu_stats.log
artifacts/gpu_metrics/
gpu_metrics/

# Optimization testing results
artifacts/optimization/
**/progressive_results_*.json
**/train_*.jsonl
**/stop_*.flag

# OS
.DS_Store
Thumbs.db

# Track workspace settings
!.vscode/settings.json

# Note: .whl files can be large but not tracking in LFS for now
flash_attn-*.whl
*.whl
artifacts/diagnostics/device_info_cache.json
artifacts/diagnostics/device_info_cache.json
artifacts/diagnostics/device_info_cache.json
training_datasets/huggingface_documentation-images/dataset_info.json
training_datasets/huggingface_documentation-images/state.json
.gitignore
installers/dependencies/winget/ui_xaml_x64.appx
installers/dependencies/winget/vclibs_x64.appx
installers/dependencies/winget/windowsappruntime_x64.exe
installers/dependencies/winget/winget.msixbundle
