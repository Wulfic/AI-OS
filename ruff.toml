line-length = 120
target-version = "py310"

[lint]
select = ["E", "F", "B"]
ignore = [
  # Allow assert in tests
  "S101",
  # Temporarily ignore long lines during rapid iteration
  "E501",
]

[format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false